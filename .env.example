# Server port (default: 3654)
PORT=3654

# SQLite database path (default: ./data/prello.db)
# DATABASE_PATH=./data/prello.db

# API key for bearer token auth (optional; if unset, no auth required)
# Set this when deploying publicly (e.g., Fly.io)
# PRELLO_API_KEY=your-secret-key-here
